import os
import platform

system = platform.system()

def main():
    if system == "Windows":
        file = "programs/calculator.exe"
        if os.path.exists("programs/calculator.exe"):
        
    else:
        print("Error. The EyeTerminal has crashed, because file ")

if __name__ == "__main__":
